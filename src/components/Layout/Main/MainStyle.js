import { makeStyles } from '@material-ui/core';

const ManiStyle = makeStyles(() => ({
  root: {
    display: 'flex',
    width: '100%',
    height: '100vh',
    backgroundColor: '#fafafa',
    justifyContent: 'center',
  },
}));

export default ManiStyle;
